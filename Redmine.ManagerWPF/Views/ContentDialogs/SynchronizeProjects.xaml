<ui:ContentDialog x:Class="Redmine.ManagerWPF.Desktop.Views.ContentDialogs.SynchronizeProjects"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:ui="http://schemas.modernwpf.com/2019"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:vm="clr-namespace:Redmine.ManagerWPF.Desktop.ViewModels"
                   xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
                  mc:Ignorable="d"
                  Title="Synchronizacja projektów"
             d:DesignHeight="200" d:DesignWidth="400"
                  PrimaryButtonCommand="{Binding SynchronizeProjectsCommand}"
                  CloseButtonText="{Binding CancelButtonText}"
                  CloseButtonCommand="{Binding CancelCommand}"
                  PrimaryButtonText="{Binding PrimaryButtonText}"
                  PrimaryButtonClick="ContentDialog_PrimaryButtonClick">
    <ui:ContentDialog.DataContext>
        <vm:SynchronizeProjectsViewModel />
    </ui:ContentDialog.DataContext>
    <ui:ContentDialog.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </ui:ContentDialog.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ui:ProgressBar
                    IsIndeterminate="False"
                    VerticalAlignment="Center"
                    Value="{Binding ProgressBarValue}"
                    Margin="0,0,0,10" />
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Zsynchronizowano:" />
            <TextBlock Margin="5,0,5,0" Text="{Binding Value}" />
            <TextBlock Text="/" />
            <TextBlock Margin="5,0,0,0" Text="{Binding TotalProjectsCount}" />
        </StackPanel>
        <TextBlock Margin="0,10,0,0" Foreground="Green" Visibility="{Binding ShowOk, Converter={StaticResource BooleanToVisibilityConverter}}"  Grid.Row="2" Text="Synchronizacja zakończona powodzeniem." />
    </Grid>
</ui:ContentDialog>