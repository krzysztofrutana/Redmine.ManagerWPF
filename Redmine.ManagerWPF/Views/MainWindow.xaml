<Window
    x:Class="Redmine.ManagerWPF.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Redmine.ManagerWPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    xmlns:vm="clr-namespace:Redmine.ManagerWPF.Desktop.ViewModels"
    Title="MainWindow"
    Width="800"
    Height="450"
    ui:WindowHelper.UseModernWindowStyle="True"
    mc:Ignorable="d">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_Synchronizacja">
                    <MenuItem Command="{Binding SynchronizeProjectsCommand}" Header="_Synchronizuj projekty" />
                    <MenuItem Command="{Binding SynchronizeIssuesCommand}" Header="_Synchronizuj zadania" />
                </MenuItem>
                <MenuItem Header="_Opcje">
                    <MenuItem Command="{Binding OpenSettingsDialogCommand}" Header="_Ustawienia" />
                </MenuItem>
            </Menu>
        </DockPanel>
        <StackPanel
            Grid.Row="1"
            Grid.Column="0"
            HorizontalAlignment="Stretch"
            Orientation="Horizontal">
            <TextBlock FontSize="30" Text="Projekt:" />
            <ComboBox
                MinWidth="300"
                Margin="10,0,0,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center">

                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,10"
                                FontSize="14"
                                FontWeight="Bold" />
                            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                                <TextBlock FontSize="12" Text="Data rozpoczęcia:" />
                                <TextBlock FontSize="12" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <!--<muxc:TreeView
            Grid.Row="2"
            Grid.Column="0"
            MinHeight="400"
            VerticalAlignment="Stretch"
            ItemsSource="{x:Bind ViewModel.Issues}"
            SelectedItem="{x:Bind ViewModel.SelectedNode, Mode=TwoWay}">
            <muxc:TreeView.ItemTemplate>
                <DataTemplate x:DataType="treeModels:TreeModel">
                    <muxc:TreeViewItem Content="{x:Bind Name}" ItemsSource="{x:Bind Children}" />
                </DataTemplate>
            </muxc:TreeView.ItemTemplate>
        </muxc:TreeView>
        <uc:IssueForm Grid.Row="2" Grid.Column="1" />-->
    </Grid>
</Window>